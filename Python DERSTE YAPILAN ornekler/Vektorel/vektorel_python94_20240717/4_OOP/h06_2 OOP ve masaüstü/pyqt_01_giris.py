from PyQt6.QtWidgets import *
# Uygulama oluşturma
app = QApplication([])
# buraya araçlar(widgets) ekleme
label = QLabel('Merhaba!')
label.show()
# Uygulamayı çalıştırma
app.exec()

xxx = QApplication([])
label = QLabel('Merhaba2!')
label.show()
xxx.exec()